import 'package:fpdart/fpdart.dart';
import 'package:pulse/core/common/entities/add_comment.dart';
import 'package:pulse/core/common/entities/comment.dart';
import 'package:pulse/core/error/failures.dart';
import 'package:pulse/core/usecase/usercase.dart';
import 'package:pulse/features/comments/domain/repository/comments_repository.dart';

/* class ReportCommentUc implements UseCase<Unit, ReportCommentParams> {
  final CommentsRepository commentsRepository;

  const ReportCommentUc(this.commentsRepository);

  @override
  Future<Either<Failure, Unit>> call(ReportCommentParams params) async {
    return 
    //await commentsRepository.addComment(params.commentId, params.reportReason);
  }
}

class ReportCommentParams {
  final int commentId;
  final String reportReason;

  const ReportCommentParams(this.commentId, this.reportReason);
} */
